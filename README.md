# logsift

A small command-line tool for parsing, filtering, and analyzing log files.

logsift reads common log formats (apache, nginx, and line-delimited JSON),
lets you filter records with simple expressions, and prints useful summary
statistics. It is designed to be fast, dependency-light, and easy to drop
into a shell pipeline.

## Status

Early development. Interfaces are still changing.
